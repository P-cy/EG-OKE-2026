[pytest]
testpaths = tests
asyncio_mode = auto
# ตั้งให้ชัด — ไม่งั้น pytest-asyncio เตือน DeprecationWarning ทุกครั้งที่รัน
asyncio_default_fixture_loop_scope = function
filterwarnings =
    ignore::DeprecationWarning:pymongo.*
